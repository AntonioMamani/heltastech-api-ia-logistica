services:
  ia-agente-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ia-agente-backend
    restart: unless-stopped
    ports:
      - "3002:3002"
    env_file:
      - .env
    environment:
      - NODE_ENV=production