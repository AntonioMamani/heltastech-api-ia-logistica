services:
  ia-agente-backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ia-agente-backend
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-4004}:3002"
    env_file:
      - .env
    environment:
      - NODE_ENV=production